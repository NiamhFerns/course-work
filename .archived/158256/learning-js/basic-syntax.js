const canvas = document.getElementById('canvas');
const ctx = canvas.getContext('2d');

// Colours/Style
// ctx.fillStyle = 'color';
// ctx.lineWidth = n;
// ctx.strokeStyle = "style";

// Rectangles
// ctx.fillRect(x, y, width, height);          -- This gives a solid rectangle.
// ctx.strokeRect(x, y, width, height);        -- This gives a rectangle border.
// ctx.clearRect(x, y, width, height);         -- This places empty space in the shape of a rectangle.

// Text
// ctx.fillText("string", x, y);
// ctx.strokeText("string", x, y);

// Paths
// ctx.beginPath();
// ctx.moveTo(x, y);                           -- Moves the "cursor" to a location.
// ctx.lineTo(x, y);                           -- Draws a line to a location.
// ctx.stroke();                               -- Fills a path with a stroke.
// ctx.closePath();                            -- Draws a line to the beginning.
// ctx.fill();                                 -- Will fill in the path if it is closed.
//
// Arcs
// ctx.arc(centreX, centreY, radius, startingTheta, endingTheta)

// JSON FUNCTIONS!!
// let stringPerson = JSON.stringify(person);
// let parsePerson = JSON.parse(stringPerson);
